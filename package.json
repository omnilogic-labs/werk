{
  "name": "werk",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build": "bun scripts/build.ts",
    "typecheck": "bun scripts/typecheck.ts",
    "test": "bun test packages/palette/test packages/terminal/test packages/session/test packages/session-daemon/test packages/werk/test packages/terminal-beamterm/test packages/workspace/test packages/mapper/test",
    "test:artefacts": "bun scripts/check-artefacts.ts",
    "check:platform": "bun scripts/check-platform-code.ts",
    "browser:install": "bun scripts/browser-install.ts",
    "test:browser": "bun run build && bun run --cwd examples/session-web test",
    "test:soak": "bun scripts/session-soak.ts"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
