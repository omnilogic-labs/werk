# M4 — `wp serve`: the browser runs the daemon's emulator

What the proposal's §7 M4 row asks: `wp serve` with a session list and one
live terminal driven by state transfer plus per-consumer dirty rows, keys
and mouse through the WASM encoders, rendered by `ghostty-web` rebased onto
the pinned artifact. The stop condition has two halves: "the browser cannot
decode what the daemon encodes, or the renderer route costs more than the
rest of the PoC combined". The milestone was built in two halves and this
file covers both: the daemon side, the web server, the page and a
deliberately minimal renderer written to prove the round trip and to be
measured, then [the renderer route](#the-renderer-route-ghostty-web-rebased)
— `ghostty-web` read, measured and rebased onto the seam behind the same
interface, with the two renderers compared side by side. The verdict on
both halves is [at the end](#m4-verdict).

**The first half of the stop condition is not hit.** The browser decodes
what the daemon encodes: the same `ghostty-vt-small.wasm` bytes, served by
the daemon's own process, instantiated in Chrome, fed the daemon's
`GHOSTSNP` snapshot and then the raw output after it, hold a replica whose
viewport text is byte-identical to the daemon's `screen` after `vim`, after
keys typed through the page, after a resize, after a lag and resume, in
two tabs at once, and on a corpse. The evidence is in
[the browser run](#the-browser-run) and the tests.

The code is under [`../src/web/`](../src/web): `server.ts`, `pages.ts`,
`wire.ts`, `build.ts`, and the page under `client/`. The daemon changes are
in `protocol/index.ts`, `daemon/session.ts`, `daemon/connection.ts`,
`daemon/server.ts` and `client/index.ts`. `bun test` is 144 across 16
files in about 35 s (the vendored `ghostty-web` clone carries a suite of
its own, which `bunfig.toml` keeps out), and no daemon, `wp serve` or
browser is left behind.

## Environment

| Item    | Value                                                                                     |
| ------- | ----------------------------------------------------------------------------------------- |
| Bun     | 1.3.14 (`~/.bun/bin/bun`); not re-run on 1.4.x for this milestone                         |
| Kernel  | `6.18.33.2-microsoft-standard-WSL2`                                                       |
| Engine  | `ghostty-wasm`, pinned commit `3c1ef5b3…`, as M1 left it; the same bytes in the page      |
| Browser | Chrome for Testing, headless, driven by `agent-browser` 0.33.1 over the DevTools protocol |
| Binary  | The compiled `dist/wp` (95.5 MB) served the browser run, from a directory with no sources |

## The design as built

**Snapshot on attach.** `attach` gained `mode: "vt" | "snapshot"`. In
snapshot mode `Session.attach` encodes the emulator inside the attach and
sends the bytes as a new frame type, `snapshot` (4), in place of `render`;
the lag → resume path sends a fresh `snapshot` the same way, through one
`Session.paint(mode)` that the attach, the resume and a new `repaint`
request all use. The ordering that makes a replica exact rather than
approximately right is by construction: `attach` registers the client,
encodes, and enqueues the frame in one synchronous step, and the PTY's
`data` callback cannot run in the middle of it, so every `output` frame
after the snapshot carries bytes the emulator consumed after the encode.
The two-request dance M3 warned about (attach, then `snapshot`, with the
output in between applied twice) is gone. `Session.encode()` is the read;
`snapshot()` is the checkpoint that clears `dirty`, and the `snapshot`
request now uses the read, which its comment had claimed all along.
Corpses take the same path: a restored corpse's emulator encodes, a
mismatch corpse has nothing to encode and sends its placeholder as a
`notice` with no frame. The CLI stays on `vt`.

**`wp serve`.** `Bun.serve` on `127.0.0.1`, port 0 by default (`--port` to
fix), printing a URL with a one-time token in the query string. The first
request carrying the token gets a `302` to the same path and an `HttpOnly;
SameSite=Strict` cookie; every other request, the WebSocket upgrade
included, needs the cookie or gets `403`. `GET /` is the session list from
`ls`, server-rendered, with a two-second refresh of the table body from
`/api/ls`; `GET /s/:id` is the terminal page; `GET /app.js` the bundle;
`GET /wasm` the pinned bytes with `application/wasm`, read from the same
embedded path the daemon loads, so the compiled binary serves them with
nothing on disk. `WS /ws/:id?cols=&rows=` is one connection to the daemon
and one snapshot-mode attach per socket. Server to page: binary messages
tagged with a first byte (0 output, 1 snapshot) and JSON notices (`hello`,
`exited`, `lagging`, `resumed`, `notice`, `effect`, `error`). Page to
server: binary is input for the PTY, JSON is `{t:"resize"}`. `/api/ws`
reports each socket's backpressure accounting.

**The page.** Fetches `/wasm`, instantiates it through the same
`GhosttyWasmEngine.load` the daemon uses, measures its cell size off a
canvas, computes the grid from the container, opens the socket at that
size. A `Replica` (`client/replica.ts`, no DOM) owns the terminal: on a
snapshot it disposes the old terminal, runs `decodeState` and `ready()`,
takes one render consumer, paints its first frame synchronously, and then
pulls `next()` pages one per `setTimeout(0)` so the first paint is
presented before history finishes; live output is written into the
terminal and a paint is coalesced through `requestAnimationFrame`. Keys go
`keydown` → `keyEventFromDom` (the W3C `code`, the `key` as the text, the
modifiers) → `engine.encodeKey(ev, replica.modes())` → the socket; mouse
the same way through `encodeMouse`, only while the replica's modes report
a tracking mode. Paste is the one extra: the `paste` event sends the text,
bracketed when DEC 2004 is on. Resize measures the container, resizes the
replica locally (the decoded terminal reflows exactly as the daemon's
does, findings/m1.md) and tells the daemon. The status line shows the
decode timings, the paint costs and the lag state; a bell flashes the
background. Everything measured is on `window.__wp`.

**The renderer** sits behind a small interface so the second half can
replace it:

```ts
interface Renderer {
  readonly cell: { width: number; height: number };
  paint(frame: Frame): void; // frame().changed rows, cursor, dirtyAll
  resizeTo(cols: number, rows: number): void;
  dispose(): void;
  readonly stats: PaintStats; // last / max / full / one-row paint cost
}
```

`CanvasRenderer` is a monospace grid on one canvas: for each changed row
it clears the row, fills non-default backgrounds (inverse swaps), draws
each cell's text with bold/italic through the font string, underline and
strikethrough as one-pixel lines, wide cells across two columns with the
spacer skipped, and the cursor in its four styles over the cell, keeping a
shadow copy of painted rows so the row the cursor left can be repainted.
xterm's sixteen colours, the cube and the grey ramp computed on demand.
That is all it is; the list of what it does not do is
[below](#what-the-minimal-renderer-does-not-do).

**The bundle.** `bun run build:web` runs `Bun.build` with
`target: "browser"` on `client/main.ts` and writes `src/web/bundle/app.js`
(104,626 B unminified with both renderers in it); `server.ts` imports that file
`with { type: "text" }`, which `bun build --compile` embeds like the wasm,
so `bun run build` runs the bundle first and the compiled `wp` carries it.
The built file is gitignored; `bundle/app.d.ts` lets tsc resolve the import
without it, and `server.test.ts` builds it if it is missing.

## Does the browser decode what the daemon encodes?

Yes, and the same bytes decode the same way at three distances from the
daemon:

| Where the decode ran                                           | What was compared                                                                                                                                                              | Result                |
| -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------- |
| In-process, `daemon/attach-snapshot.test.ts`                   | Replica `plainText` and cursor vs `screen` after 3,000 lines, after more output, after a resize to 60×20; a lagging client's resume snapshot; an exited session's final screen | identical, all three  |
| Through `wp serve`'s socket under Bun, `web/server.test.ts`    | The page's own `Replica` fed by the socket: after history, after live output, after input typed through the socket, after a resize; two tabs; an exited session; an unknown id | identical             |
| In Chrome, from the compiled binary, [below](#the-browser-run) | `window.__wp.text()` vs `screen` for `vim`, after `:q!`, after typing, in two tabs, after a forced WebSocket lag under `yes`, on a corpse                                      | identical, every time |

The first test also checks what the mechanism promises: the first frame a
snapshot-mode attacher sees is the snapshot, no `render` ever arrives, and
the output bytes after it (23 of them, for the prompt redraw the attach's
resize provoked) applied on top keep the replica exact.

## The Bun-free loader, confirmed

`loader.ts`, `layout.ts`, `index.ts` and `encoders.ts` bundle for the
browser as they are; `bytes.ts` stays the only Bun file and the page
replaces it with `fetch("/wasm")`. The bundle has no `Bun.`, `bun:`,
`process.` or `Buffer` reference (a test greps for the first two). The
one change the browser needed was a type, not code: under the DOM lib
`WebAssembly.compile` wants a `BufferSource` whose `Uint8Array` generic
differs from Bun's, so `GhosttyModule.load` casts the bytes to
`ArrayBuffer | Uint8Array<ArrayBuffer>`, which both libs accept. The
browser files have a `tsconfig.json` of their own with `lib: [dom]` and
`types: []`, no `bun-types` at all, and `bun run typecheck` runs both
configurations; that is the type-level proof that nothing in the page
reaches for Bun. The trampoline for host functions (M1's `hostFunction`)
is not exercised in the page because a replica subscribes to no effects —
the daemon's emulator is the one that answers queries — so whether
`new WebAssembly.Module` of a forty-byte module is allowed synchronously
on Chrome's main thread remains unmeasured here; M1's note says it should
be.

Loading in Chrome: 721 KiB of wasm fetched and instantiated in 6–12 ms
(warm cache; the status line reports it), which is below the time the
first snapshot takes to arrive.

## Bun's WebSocket backpressure API, and the rule built on it

What Bun 1.3.14 gives a `ServerWebSocket`, from its types and docs:

| Item                       | What it is                                                                                                                                                            |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ws.send(data)` return     | `-1`: enqueued, but there is backpressure. `0`: dropped because of a connection issue. `1+`: bytes sent. (`sendBinary`/`sendText` the same.)                          |
| `drain(ws)` handler        | Fires when the enqueued bytes are gone                                                                                                                                |
| `backpressureLimit` option | How much Bun will hold for a socket, default 16 MiB; `closeOnBackpressureLimit` (default false) closes the socket past it, otherwise sends past it return `0`         |
| `bufferedAmount`           | **Not on the server socket.** It exists on Bun's client `WebSocket`, not on `ServerWebSocket`, so there is no way to read how much is queued between `-1` and `drain` |

So the rule is the simplest one that never needs `bufferedAmount`: the
first `-1` marks the socket lagging and starts a timer; while lagging,
output frames are dropped for that socket and counted; on `drain` the
server asks the daemon to `repaint`, keeps dropping until the snapshot
frame arrives in the daemon's stream (so nothing consumed before the
encode is forwarded), sends it, and forwards output again. The user-space
bound is therefore zero bytes on top of what the kernel and the browser
accept, which is a different shape from the daemon's 256 KiB queue over
the kernel's buffer; measured, that turned out generous enough (2.9 MB
before the first `-1`, below). `backpressureLimit` is set to 4 MiB so a
snapshot enqueued under backpressure is never the message Bun drops; in
the run, `droppedByBun` stayed 0. Two stages, one page: the daemon's
socket to the web server lags and resumes on its own with a snapshot the
server forwards; the WebSocket lags and resumes with a snapshot the
server asked for. The page shows which (`lagging (daemon)` /
`lagging (ws)`).

The measurement: `yes | head -c 40000000` (60 MB after the line
discipline's CRLF) into a page whose next message handler busy-looped for
5 s (`window.__wp.stall(5000)`, so the tab stopped draining its socket):

| Quantity                                        | Value                                                                                                                                  |
| ----------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| `ws.send` calls                                 | 4,415; two returned `-1`, none `0`                                                                                                     |
| First `-1` after                                | **2,897,200 B** accepted by the kernel and Chrome                                                                                      |
| Lag episodes / drains                           | 1 / 1                                                                                                                                  |
| First `-1` to `drain`                           | **907 ms**                                                                                                                             |
| Output dropped for the socket                   | 7,612,845 B                                                                                                                            |
| Bytes sent over the socket                      | 52,402,146                                                                                                                             |
| Snapshots sent                                  | 2: the attach's and the resume's                                                                                                       |
| The page                                        | one `lagging (ws)`, one `resumed (ws)`, both delivered together once the stall ended; text identical to the daemon's screen afterwards |
| Daemon-side lag on the socket to the web server | none                                                                                                                                   |

The drain came 907 ms into a 5 s stall, which says the browser's network
process kept reading the socket into its own buffer while the renderer
was blocked — Chrome absorbs a lot before TCP backpressure reaches Bun,
and once it does, Bun's `-1` and `drain` are prompt. The 2.9 MB absorbed
before the first `-1` is the loopback kernel buffers plus that; on a real
network it would be smaller.

## A bound the first half found in M2's rule

Opening the heavily styled session (a 1,053,913-byte snapshot, below) put
the daemon's connection to the web server into lag on the next 20 bytes
of output and earned it another megabyte on the drain:

```
conn 31: short write on a control frame (219264 of 1053918), 834654 B queued, waiting for drain
conn 31: lagging, dropped 20 bytes, 323160 control bytes still queued
conn 31: resumed with a 1053913-byte snapshot
```

The daemon's queue bound was measured against everything queued,
non-droppable paints included, so a paint larger than the bound made the
connection lag on the first droppable frame after it, and each drain
produced another paint — a loop for as long as output kept coming. The
bound now applies to the droppable bytes alone (`queuedDroppable` in
`connection.ts`); memory stays bounded because the non-droppable part is
one paint plus control messages. M2's slow-client numbers are unchanged
by it (max queue 260,973 B under the 262,144 B bound on the re-run).
Whether a product wants the bound expressed as bytes over the paint, as
time, or both, is the question M2 already left open.

## The browser run

Sessions were started with the compiled `wp` in a runtime and state
directory of their own (`XDG_RUNTIME_DIR`, `WP_STATE_DIR`), `wp serve
--port 47313` was run from a directory with no sources, and Chrome was
driven headless at 900×600, which gave a 100×31 grid at 14 px monospace.

| Step                                                                                       | Result                                                                                                                                                                                                                                                                     | Screenshot                                           |
| ------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| The token URL, then `/`                                                                    | `302` to `/` with the cookie; the list shows the three sessions with id, command, engine, status, title, age and clients, and refreshes                                                                                                                                    | [`m4/list.png`](m4/list.png)                         |
| Click through to a shell that printed a styled line and is running `vim -u NONE notes.txt` | The page attaches at 100×31 (the session was 100×30; the page's grid wins, as M2's size rule says); status line: `snapshot 2184 B · ready 4.6 ms · first paint 13.1 ms (31 rows, 0 pending) · history 7.3 ms (0 pages, 31 rows)`; vim's screen; text identical to `screen` | [`m4/vim.png`](m4/vim.png)                           |
| `:q!` Enter through the page                                                               | vim leaves the alternate screen; the primary screen shows bold red, green, underline, inverse, 256-colour orange, CJK and an emoji, `vim exited with 0` and the prompt, cursor as a block; text identical to `screen`                                                      | [`m4/after-vim.png`](m4/after-vim.png)               |
| `exit` Enter through the page                                                              | Status `exited (0)`; `wp ls` says `exited(0)`                                                                                                                                                                                                                              | —                                                    |
| `seq 1 5000` (2,000 lines of scrollback kept)                                              | `snapshot 12624 B · ready 1.0 ms · first paint 4.8 ms (552 rows, 1080 pending) · history 1.7 ms (2 pages, 1632 rows)`: the viewport painted after `ready()` with 1,080 rows still to come, and history finished 1.7 ms later in two deferred pages                         | [`m4/seq.png`](m4/seq.png)                           |
| A second tab on the same session; `echo TWOTABS` typed in it                               | Both tabs' text identical to each other and to `screen`; the daemon counts two clients; each tab painted from its own consumer                                                                                                                                             | [`m4/two-tabs.png`](m4/two-tabs.png)                 |
| 3,000 lines of per-cell 256-colour foreground and background                               | `snapshot 1053913 B · ready 1.1 ms · first paint 12.9 ms (556 rows, 1098 pending) · history 7.7 ms (2 pages, 1654 rows)`; every cell coloured as written                                                                                                                   | [`m4/styled.png`](m4/styled.png)                     |
| `yes` into a stalled tab                                                                   | The table above; the page ends on `YES-DONE` and the prompt, identical to `screen`                                                                                                                                                                                         | [`m4/yes-backpressure.png`](m4/yes-backpressure.png) |
| A program with `?1000h ?1006h` on, a click on the canvas                                   | `^[[<0;51;16M ^[[<0;51;16m` echoed by `cat -v`: the page's mouse path encodes SGR reports at the clicked cell, and only because the replica's modes said to                                                                                                                | —                                                    |
| The daemon shut down and restarted; the `seq` session's page reopened                      | Status `corpse (restored)`, `snapshot 12659 B · ready 0.8 ms · first paint 3.7 ms`, text identical to `screen`; a key typed gets `input ignored: this session is a snapshot, read-only` in the status line                                                                 | [`m4/corpse.png`](m4/corpse.png)                     |

One caveat about how keys were sent. `agent-browser`'s `keyboard type`
dispatches no `keydown` on a page whose focus is a canvas, and its `press`
delivers the physical `code` but an empty `key` for shifted punctuation,
so letters, digits, Enter, Escape, Backspace and Space went through
Chrome's real key dispatch and the page's `keydown` handler, while `:`
and `!` were dispatched as `KeyboardEvent`s carrying the `key` Chrome
would have produced. Both routes end in the same handler and the same
encoder call; the tool, not the page, is what could not produce a `:`.

## Numbers

| Measured                                                   | Value                                                                                                               |
| ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| Snapshot on the wire, plain: idle shell / 3,000-line seq   | 1,188 B / 12,624 B                                                                                                  |
| Snapshot on the wire, styled: per-cell colour, 3,000 lines | **1,053,913 B**; encoded in about a millisecond, as M3 found                                                        |
| Time to first paint in Chrome, from the snapshot's arrival | **3.2–13.1 ms** including `decodeState` + `ready()` (0.6–4.6 ms) and the first full paint; 12.9 ms for the megabyte |
| History after the first paint                              | 0.7–7.7 ms, two pages for the long sessions, in deferred tasks after the paint                                      |
| Full repaint, 100×31, JS side                              | **0.1–0.4 ms** (five runs of `window.__wp.repaint()`); the first paint of a page, cold, 6–8 ms                      |
| One-row change                                             | **0.2 ms**                                                                                                          |
| WASM fetch + instantiate in Chrome                         | 6–12 ms for 721 KiB                                                                                                 |
| Bundle                                                     | 71,182 B, unminified                                                                                                |
| WebSocket backpressure under `yes`                         | first `-1` after 2.9 MB, drain 907 ms later, 7.6 MB dropped, one resume snapshot, screen exact                      |
| Headless, under Bun, the same page core                    | snapshot 14,106 B, ready 3.4 ms, first paint 7.5 ms (654 rows, 1,348 pending), history 5.8 ms (2 pages, 2,002 rows) |

The paint numbers measure what the page's JavaScript spends issuing canvas
commands; Chrome rasterises them off the main thread afterwards, so they
are a floor for the cost of the renderer, not the cost of pixels reaching
the screen. That is the right number for "does the page keep up with
output" and the wrong one for "how heavy is this renderer", which is a
question for the second half with a real one.

## What the minimal renderer does not do

For the renderer evaluation to price, in the order a user would notice:

- **Selection.** Nothing is selectable: the canvas has no text, so the
  browser's native selection does not apply, and no selection of the
  grid's own is built. Copy is therefore absent too.
- **Scrollback scrolling.** The viewport is painted; the wheel is sent to
  the program only when a mouse mode is on and does nothing otherwise.
  `frame().viewport` carries the scrollbar shape, so the data is there.
- **Fonts and graphemes.** One font string, `14px monospace`; a cell's
  text is whatever `fillText` makes of it, clipped to its column width.
  No fallback fonts, no ligature handling, no grapheme-width correction
  beyond the wide/spacer cells libghostty reports, no box-drawing or
  Powerline glyph special-casing. Emoji drew, at the system's mercy.
- **IME and composition.** `keydown` with `isComposing` is ignored and
  nothing is composed anywhere; there is no hidden input element.
- **Links.** `hyperlink` on a cell is not read; nothing is clickable.
- **Cursor blink** is not animated; the style and visibility are drawn
  from `frame().cursor`.
- **Faint, blink, double underline, underline colour, undercurl** are
  not drawn; `Cell` does not carry the last three yet.
- **Device pixel ratio** is applied to the canvas, but the cell size is
  measured in CSS pixels and rounded up, so fractional metrics drift.
- **Focus** is document-wide keyboard capture; there is no focus ring and
  no way to give a key to the browser except Ctrl+Shift+V and F12.
- **Bell** is a background flash; no sound, no title marker.
- **Resize** is a whole repaint with no debounce.
- **Accessibility**: nothing — no text in the DOM, no live region.

## What the `ghostty-web` evaluation needs to know

- The interface to implement is `Renderer` in `client/renderer.ts`:
  `paint(frame)`, `resizeTo(cols, rows)`, `dispose()`, `cell`, `stats`.
  `main.ts` constructs it once, reads `cell` to compute the grid and to
  convert mouse pixels to cells, calls `resizeTo` before the first
  paint, and calls `paint` with every `Frame` the `Replica` produces
  (the first synchronously after `ready()`, the rest through
  `requestAnimationFrame`). A replacement needs nothing else from the
  page, and the page needs nothing else from it.
- A `Frame` is the seam's: `changed` rows with `Cell`s (`text`, `fg`,
  `bg`, bold/italic/underline/inverse/strikethrough, `width` 0/1/2),
  `dirtyAll`, the `cursor` and `cursorChanged`, the `viewport`. A
  renderer that wants raw packed `GhosttyCell`s or the render state
  handle would have to reach past the seam into
  `GhosttyWasmTerminal`; `Replica.term` is that terminal.
- The page's structure is one file, `client/main.ts`: `state` and
  `renderStatus()`; `onMessage` splitting tagged binary from JSON;
  `Replica` for decode and output; `applySize` for resize; the input
  handlers at the bottom of the file; `window.__wp` for measurement.
  `client/input.ts` is the DOM-to-seam mapping and would stay whatever
  the renderer is; `client/replica.ts` is DOM-free and tested under Bun.
- Two tabs each hold a `Replica` and a consumer of their own; the daemon
  holds one emulator and sends each socket its own snapshot. A renderer
  that keeps state across frames (the shadow rows here) is per tab by
  construction.
- What the browser side is expected to gain from `ghostty-web` is exactly
  the list above; what it must keep is the frame path, because the
  daemon's snapshot and the browser's live output are the same bytes and
  the same code, and a renderer that re-emits VT into a second emulator
  gives that up (proposal §3).
- The page attaches at its own grid and resizes the session; the CLI's
  size rule ("most recent attacher wins", M2) applies to browser tabs
  unchanged and is still a product question.

## M4, first half: verdict

**Not hit.** The browser decodes what the daemon encodes, and does so with
the two-stage split the proposal wanted: on a 5,000-line session the page
painted 1.0 ms after the snapshot arrived, with 1,080 rows still pending,
and had them 1.7 ms later. Everything in the §7 row except the renderer
is built — the list, the live terminal driven by state transfer and
per-consumer dirty rows, keys and mouse through the WASM encoders — and
the renderer that stands in for `ghostty-web` is behind an interface of
five members. What it costs to put `ghostty-web` behind that interface,
against the rest of the PoC, is the second half, below.

## The renderer route: `ghostty-web` rebased

The proposal's §3 picks the rebase of `ghostty-web` onto the pinned
upstream artifact as the PoC's renderer route, with re-emission into
xterm.js as the fallback, and asks for one piece of evidence: how much of
ghostty-web's renderer survives a rebase, and how much of it would be
worth keeping. This section is that evidence. The clone is described in
[`../vendor/README.md`](../vendor/README.md) (commit `1858a594…`, the
default branch on 2026-06-28, release 0.4.0, MIT); the port is
`client/renderer-ghostty-web.ts` and `client/selection-ghostty-web.ts`,
selected by `?renderer=ghostty-web` on the terminal page, with the
minimal renderer still the default.

### What ghostty-web is

A browser terminal with an xterm.js-shaped API over Ghostty's VT parser,
built from a Ghostty submodule pinned in December 2025 plus a 1,620-line
patch (`patches/ghostty-wasm-api.patch`) that adds its own
`include/ghostty/vt/terminal.h` and the Zig behind it. The patch's API is
the shape of the whole library: `ghostty_terminal_new(cols, rows)`,
`ghostty_render_state_update` returning a dirty enum, one
`ghostty_render_state_get_viewport` call filling a packed buffer of
`GhosttyCell`s (a codepoint, foreground and background **already resolved
to RGB** inside the WASM, a flags byte, a width, a hyperlink id, a
grapheme count), per-row dirty flags, `ghostty_terminal_get_scrollback_line`
and `_length`, `_get_hyperlink_uri`, `_is_alternate_screen`, `_get_mode`,
and the key encoder. None of it is upstream's `terminal.h`; upstream's
render state, selection, formatter and scroll API do the same jobs with a
different shape and, for selection and scrolling, do more.

Code lines per area, comments and blanks excluded, tests aside:

| Area                              | File(s)                                                          |     Lines | What it does                                                                                                                                                                                                                                                                             |
| --------------------------------- | ---------------------------------------------------------------- | --------: | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| xterm-shaped API                  | `terminal.ts`                                                    |     1,164 | `Terminal`: `open`, `write` with a resize queue, `onData`/`onResize`/`onTitleChange`/…, options, addons, the render loop on `requestAnimationFrame`, and the viewport: `viewportY` in JavaScript, smooth-scroll animation, scrollbar drag and fade, wheel handling                       |
| Keyboard, paste, IME, mouse       | `input-handler.ts`                                               |       773 | A `KeyboardEvent.code` → USB HID key table, a wrapper over Ghostty's key encoder, paste/`beforeinput`/composition de-duplication, and SGR/X10 mouse reports **encoded in JavaScript**                                                                                                    |
| Selection                         | `selection-manager.ts`                                           |       670 | Mouse drag with a threshold, double-click word, triple-click line, auto-scroll at the edges, absolute-row coordinates, text extraction by walking cells, clipboard, a hidden textarea for the context menu                                                                               |
| Renderer                          | `renderer.ts`                                                    |       631 | Canvas 2D on one canvas: `M`-based font metrics, two-pass rows (backgrounds, then text), adjacent rows repainted for glyph overflow, selection and hover-link colours inlined, cursor in three styles with blink, a scrollbar, scrollback rows composed onto the screen from `viewportY` |
| WASM binding                      | `ghostty.ts`                                                     |       597 | The patch's API marshalled by hand: 44 distinct `ghostty_*` symbols, fixed offsets for the packed cell, a reused viewport buffer                                                                                                                                                         |
| Types                             | `types.ts`                                                       |       592 | The packed cell, the flags, the key enum and modifier bits, the encoder options                                                                                                                                                                                                          |
| Links                             | `link-detector.ts`, `providers/*`                                |       382 | OSC 8 links through the patch's URI lookup, URLs by regex over buffer lines, hover and click                                                                                                                                                                                             |
| xterm buffer API                  | `buffer.ts`                                                      |       263 | `term.buffer.active.getLine(y).getCell(x)` over the packed cells                                                                                                                                                                                                                         |
| xterm interfaces, fit addon, misc | `interfaces.ts`, `addons/fit.ts`, `event-emitter.ts`, `index.ts` |       381 | The xterm type surface, `FitAddon`, `init()` loading the WASM                                                                                                                                                                                                                            |
| **Total**                         |                                                                  | **8,375** | plus 7,177 lines of tests, of which 3,203 for `terminal.ts`                                                                                                                                                                                                                              |

Of that, roughly 2,000 lines (the `Terminal` class, the buffer API, the
interfaces, the fit addon, the emitter) exist to be xterm.js-compatible
and do nothing a page of our own needs; roughly 1,200 (`ghostty.ts`,
`types.ts`) are a binding to an API the pinned upstream no longer has;
and roughly 1,000 (the key table, the encoder wrapper, the mouse
encoding, the mode queries) duplicate what M1's adapter already does
through upstream's own encoders and `modes()`. What is left — the
renderer, the selection manager, the link detection, the IME handling —
is about 1,800 lines, and that is the part the proposal's question is
about.

**Its emulator seam** is small and named: the renderer draws through
`IRenderable` (`getLine(y)`, `getCursor()`, `getDimensions()`,
`isRowDirty(y)`, `needsFullRedraw()`, `clearDirty()`,
`getGraphemeString(y, x)`) and, for scrolled views, `IScrollbackProvider`
(`getScrollbackLine(offset)`, `getScrollbackLength()`), with the viewport
offset passed in as a number. The selection manager reaches wider: into
`Terminal` for `viewportY`, `cols`, `rows` and `scrollLines`, into the
renderer for the canvas and metrics, into the binding for scrollback
length, lines and graphemes. Those nine methods and that handful of
fields are what a rebase re-targets.

### How the rebase was done

The `Frame` from the seam is a different shape from `IRenderable`: it
carries the rows that changed rather than answering `getLine(y)` for any
row, its rows are already whatever the viewport shows (libghostty scrolls
the viewport itself; a scrolled frame is `dirtyAll` with the new rows),
its colours are the seam's `Color` rather than resolved RGB, a cell's text
is the whole grapheme, and the cursor's shape is the terminal's. So the
port keeps a shadow copy of the rows as painted — ghostty-web's
`getLine` answered from that — and re-targets each area:

| Area of `renderer.ts`                                   | Fate                                                                                                                                                                                                        |
| ------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Font metrics from `M`, DPI-scaled canvas sizing         | Kept as is                                                                                                                                                                                                  |
| Two-pass row painting, adjacent-row repaint             | Kept as is, over the shadow rows                                                                                                                                                                            |
| Cell backgrounds, text, bold/italic, underline, strike  | Kept; colours re-targeted from RGB fields to `Color` through the theme palette; inverse handled by swapping the two `Color`s; faint and invisible dropped because `Cell` does not carry them yet            |
| Grapheme lookup (`getGraphemeString`)                   | Dropped: `Cell.text` is already the cluster                                                                                                                                                                 |
| Cursor: block with accent, underline, bar; blink timer  | Kept; the style now comes from `frame.cursor.style` (DECSCUSR) with a hollow block added, and the blink timer follows `cursor.blinking` rather than an option                                               |
| Selection colours and `isInSelection`                   | Kept as is; the coordinates are pushed in by the selection controller rather than pulled from a manager                                                                                                     |
| Hover underlines for OSC 8 and regex links              | Dropped: `Cell` has no hyperlink id, and the detector was not ported                                                                                                                                        |
| Scrollback composition from `viewportY`                 | **Dropped: superseded.** libghostty's `ghostty_terminal_scroll_viewport` moves the viewport and the render state reports the new rows; 60-odd lines of scrollback/screen row arithmetic have no counterpart |
| Scrollbar with fade                                     | Kept; re-targeted from `viewportY`/`scrollbackLength` onto `frame.viewport` (`total`, `offset`, `rows`, `active`)                                                                                           |
| `setTheme`, `setFontSize`, `setFontFamily`, `remeasure` | Dropped: xterm-shaped options surface                                                                                                                                                                       |

| Area of `selection-manager.ts`                                                                         | Fate                                                                                                                                                                                                         |
| ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Mouse state machine: press, threshold, drag, document-level move/up, edge auto-scroll                  | Kept as is                                                                                                                                                                                                   |
| Absolute-row model, normalisation to viewport rows                                                     | Kept; the absolute row is `frame.viewport.offset + viewportRow`, which is also the row space of libghostty's `SCREEN` points                                                                                 |
| Double-click word, triple-click line                                                                   | Kept, over the shadow rows (viewport only; upstream read scrollback lines too)                                                                                                                               |
| Text extraction by walking cells and graphemes                                                         | **Dropped: superseded.** `ghostty_terminal_selection_format_alloc` formats two grid references as Ghostty's own copy does — soft-wrapped rows rejoined, trailing whitespace trimmed — and reaches scrollback |
| Clipboard: `ClipboardItem`, `writeText`, `execCommand`                                                 | Kept; the permanent hidden textarea became one made on demand                                                                                                                                                |
| Context-menu textarea trick, `select`/`selectLines`/`selectAll`/`getSelectionPosition`, change emitter | Dropped: xterm-shaped                                                                                                                                                                                        |

Nothing of `terminal.ts`, `ghostty.ts`, `types.ts`, `buffer.ts`,
`input-handler.ts`, the link code or the addons was ported: the page's
`input.ts` (130 lines) plus M1's encoders already do what
`input-handler.ts` does, and the rest is either the xterm API or the
patch's binding. The wheel rule from `terminal.ts` — arrow keys on the
alternate screen, viewport scrolling on the primary — was rewritten in
`main.ts` in twenty lines over `replica.scrollViewport`; the smooth-scroll
animation and scrollbar drag were not. Ghostty's own habit of scrolling
back to the bottom on a keypress was added, since libghostty keeps a
scrolled viewport where it is while output arrives (a test confirms it).

The adapter gained what the rebase needed and upstream provides:
`scrollViewport` (top, bottom, delta, row), `activeScreen`, and
`selectionText` between two points in screen or viewport space
(`engine/ghostty-wasm/index.ts`, 119 lines, `viewport.test.ts`). `Replica`
passes them through when the terminal has them.

### What survived, counted

Counting code lines that appear verbatim in the port after normalising
quotes and whitespace:

| File                      | Upstream code lines | Port code lines | Verbatim from upstream | Share of upstream reused | Share of the port that is upstream's |
| ------------------------- | ------------------: | --------------: | ---------------------: | -----------------------: | -----------------------------------: |
| `renderer.ts`             |                 503 |             429 |                    207 |                      41% |                                  48% |
| `selection-manager.ts`    |                 508 |             359 |                    233 |                      45% |                                  64% |
| everything else in `lib/` |               7,364 |               0 |                      0 |                       0% |                                    — |

So of ghostty-web's 8,375 code lines, 440 came across verbatim and
another 350 or so came across rewritten; of the 1,620-line patch, nothing
was absorbed, because every call it added has an upstream counterpart the
M1 adapter already wraps, and two of those counterparts (selection
formatting, viewport scrolling) replaced ghostty-web code rather than
supporting it.

### Cost so far, and to reach the gap list

| Item                           |                                                                                                                                                                    Value |
| ------------------------------ | -----------------------------------------------------------------------------------------------------------------------------------------------------------------------: |
| Files created                  |                                                                                                                                    2 client files, 1 test, `bunfig.toml` |
| Files changed                  |                                                   `ghostty-wasm/index.ts`, `client/main.ts`, `client/replica.ts`, `vendor/README.md`, `web/README.md`, both ignore files |
| Lines added                    |                                                          about 1,850 (1,570 in new files, of which 330 are licence, provenance and per-area notes; 290 in existing ones) |
| Effort, my own estimate        | reading and measuring ghostty-web 2 h; adapter additions 1 h; renderer port 2 h; selection port 1.5 h; page wiring and the browser run 2.5 h — **about one working day** |
| The rest of the PoC, for scale |                                                                                           9,800 non-test and 3,400 test lines under `src/` before this step, over M0–M4a |

What remains to reach the [gap list](#what-the-minimal-renderer-does-not-do)
with this port, estimated from what ghostty-web has and what it lacks:

| Gap                                                                    | ghostty-web has                                                                                            | Port has                                   | Remaining, estimated                                                                                                                                                                                                                            |
| ---------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Selection and copy                                                     | Drag, word, line, auto-scroll, clipboard                                                                   | All of it, viewport-anchored word and line | Word and line over scrollback rows through a `SCREEN`-point row read, or `ghostty_terminal_selection_adjust`; half a day                                                                                                                        |
| Scrollback scrolling                                                   | Wheel, smooth scroll, scrollbar drag and fade, page keys                                                   | Wheel and fade; keys return to the bottom  | Scrollbar drag, keyboard paging, smooth scroll if wanted; a day                                                                                                                                                                                 |
| Fonts and graphemes                                                    | `M`-box metrics + 2 px, `fillText` of the cluster, no fallback, no box-drawing or Powerline special-casing | The same, and the defect below             | This is where ghostty-web is no further than the minimal renderer: line-box metrics rather than `M`-box, fallback fonts, box-drawing and block glyphs drawn rather than rasterised, ligature policy. Days, and it is the long tail §3 warned of |
| IME and composition                                                    | `compositionstart/update/end` and `beforeinput` handling, ~100 lines                                       | Nothing                                    | Port those onto `input.ts` with a hidden input element; a day, plus testing on real IMEs                                                                                                                                                        |
| Links                                                                  | OSC 8 through the patch's URI lookup, regex URLs, hover, click                                             | Nothing                                    | `Cell.hyperlink` from the render state's style, the URI through upstream's hyperlink query, then the detector and hover underline port; a day                                                                                                   |
| Cursor blink                                                           | Timer over an option                                                                                       | Timer over `cursor.blinking`               | Done                                                                                                                                                                                                                                            |
| Faint, blink, invisible, double underline, undercurl, underline colour | Faint and invisible only                                                                                   | Neither, pending `Cell` fields             | `Cell` fields from `GhosttyStyle`, then a few lines each; half a day                                                                                                                                                                            |
| Device pixel ratio                                                     | Same `Math.ceil` rounding as the minimal renderer                                                          | Same                                       | Fractional metrics in device pixels; half a day                                                                                                                                                                                                 |
| Focus, bell, resize debounce, accessibility                            | Nothing beyond the minimal renderer                                                                        | Nothing                                    | Not in ghostty-web either                                                                                                                                                                                                                       |

Around a week, then, to bring the port to the whole list — and most of
that week is the same work an own renderer would need, because ghostty-web
does not have it either.

### Side by side

The same compiled `wp` and the same `agent-browser` run as the first
half, at 900×600. The ghostty-web renderer measures a 9×15 cell at 14 px
where the minimal one measures 9×18, so the same page attaches at 100×38
rather than 100×31; both grids are the page's, as before.

| Check                                                                           | Minimal renderer                                                                 | ghostty-web renderer                                                                                                                                                                                                                                                                                                                                                                                                                                             | Screenshots                                                                                                                          |
| ------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| Bold red, green, underline, inverse, italic, strike, 256-colour, RGB background | All drawn                                                                        | All drawn; text sits on an alphabetic baseline and looks the same at a glance                                                                                                                                                                                                                                                                                                                                                                                    | [`m4/rebase-minimal-text.png`](m4/rebase-minimal-text.png), [`m4/rebase-ghostty-web-text.png`](m4/rebase-ghostty-web-text.png)       |
| `中文字 日本語 🙂🎉 wide-then-narrow abc`                                       | Wide cells across two columns, emoji at the system's mercy                       | The same                                                                                                                                                                                                                                                                                                                                                                                                                                                         | same                                                                                                                                 |
| A box: `┌──┬──┐ / │ab│cd│ / ├──┼──┤ / └──┴──┘`                                  | Drawn, with the gaps between glyphs the system monospace font leaves             | **The horizontal segments are missing** in the three rows that have them; the verticals show. The rows are painted twice (a dirty row and its neighbour), and a probe of the same font shows `│` inking from 2 px above the cell to 2 px past it and `🎉` and `中` likewise, so glyphs overflow ghostty-web's `M`-derived 15 px cell in both directions and the neighbour's repaint clears part of them. That is the likely mechanism; it was not chased further | same                                                                                                                                 |
| `vim -u NONE notes.txt`                                                         | As the first half                                                                | The same screen, 38 rows                                                                                                                                                                                                                                                                                                                                                                                                                                         | [`m4/rebase-minimal-vim.png`](m4/rebase-minimal-vim.png), [`m4/rebase-ghostty-web-vim.png`](m4/rebase-ghostty-web-vim.png)           |
| Selection: a drag from `bold red` across into the next row, as DOM mouse events | Nothing to select                                                                | Highlighted in the theme's inverse colours; copied text `bold red green underline inverse italic strike 256-orange rgb-bg\n$ echo '中文字 日本語 🙂🎉 wide-then-`, i.e. two rows joined with a newline, from libghostty's formatter                                                                                                                                                                                                                              | [`m4/rebase-ghostty-web-selection.png`](m4/rebase-ghostty-web-selection.png)                                                         |
| Double-click on `wide-then-narrow`; triple-click on that line                   | —                                                                                | `wide-then-narrow`; `中文字 日本語 🙂🎉 wide-then-narrow abc` — the CJK and emoji come back as text, not as `?`                                                                                                                                                                                                                                                                                                                                                  | —                                                                                                                                    |
| A programmatic selection over the box's two middle rows                         | —                                                                                | `│ab│cd│\n├──┼──┤`                                                                                                                                                                                                                                                                                                                                                                                                                                               | —                                                                                                                                    |
| `seq 1 5000`, then ten wheel ticks of 100 px up                                 | The wheel does nothing; the viewport stays at the bottom (`offset 1732 of 1763`) | The viewport moves 66 rows (`offset 1687 → 1621`, `active: false`), the first row reads `4898`, each scroll paints the full 38 rows in 0.6 ms, the scrollbar shows                                                                                                                                                                                                                                                                                               | [`m4/rebase-minimal-seq.png`](m4/rebase-minimal-seq.png), [`m4/rebase-ghostty-web-scrolled.png`](m4/rebase-ghostty-web-scrolled.png) |
| `scrollTo("top")`, select rows 0–2 there                                        | —                                                                                | `offset 0`, first row `3277` (the oldest retained), selection text `3277\n3278\n3279`: screen-space points reach scrollback                                                                                                                                                                                                                                                                                                                                      | —                                                                                                                                    |
| A real keypress while scrolled to the top                                       | —                                                                                | The viewport returns to the bottom (`offset 1687`, `active: true`) and the key arrives                                                                                                                                                                                                                                                                                                                                                                           | —                                                                                                                                    |
| Wheel on the alternate screen                                                   | Nothing                                                                          | Arrow keys are sent, up to five per event (ghostty-web's rule; not exercised in the run beyond the code path)                                                                                                                                                                                                                                                                                                                                                    | —                                                                                                                                    |
| Console errors during the run                                                   | none                                                                             | none                                                                                                                                                                                                                                                                                                                                                                                                                                                             | —                                                                                                                                    |

Paint cost, measured as the first half did (`window.__wp.repaint()` five
times; one character typed for the one-row case):

| Measured                                | Minimal, 100×31 |                                   ghostty-web, 100×38 |
| --------------------------------------- | --------------: | ----------------------------------------------------: |
| Full repaint, JS side                   |      0.2–0.7 ms |                                            0.7–1.0 ms |
| One-row change                          |      0.0–0.2 ms | 0.2 ms, painting 3 rows (the row and both neighbours) |
| Full repaint after a wheel scroll       |               — |                                                0.6 ms |
| First paint from the snapshot's arrival |          3.6 ms |                                                7.0 ms |
| Bundle                                  |        71,182 B |                   104,626 B with both renderers in it |

Neither number is a problem; the ghostty-web loop does about twice the
work per row (a clear, a fill, two passes, neighbours) and it shows.

### The three routes, compared

| Route                                     | What it gives, as measured or read                                                                                                                                                                                                                                       | What it costs                                                                                                                                                                                                                                                                                          |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Minimal renderer** (first half)         | Text, colours, the five attributes `Cell` carries, wide cells, four cursor styles; 0.2–0.7 ms a repaint; 257 lines                                                                                                                                                       | Everything on the gap list                                                                                                                                                                                                                                                                             |
| **ghostty-web rebased** (this half)       | The above plus selection with copy through libghostty's formatter, scrollback scrolling with a scrollbar, cursor blink, glyph-overflow repainting, a theme; 0.7–1.0 ms a repaint; 790 code lines in the page plus 120 in the adapter, 440 of them ghostty-web's verbatim | About a day so far; about a week more to the whole gap list, most of it work ghostty-web has not done either. One visible defect inherited (box-drawing horizontals). No dependency: nothing of its patch, binding, `Terminal` or input code was needed, and its Ghostty pin is irrelevant to the port |
| **Re-emit into xterm.js** (§3's fallback) | Not built. From §3 and the first half: fonts with fallback and ligature policy, selection, IME, links, search, fit, accessibility, all today; the browser gets the VT bytes the CLI's `emitVt` already produces                                                          | A second emulator in the browser, no state transfer there, the soft-wrap defect on every browser resize (`findings/m1.md`), and per-consumer dirty rows unused; xterm.js itself is 60k-plus lines to carry and the seam gains nothing from it                                                          |

### Speculation for after the PoC

The proposal's §8 row reads "rebasing `ghostty-web` costs more than
writing a renderer → the renderer is werk's own from the start". The
measurement does not fall cleanly on either side of that row, because
"rebasing ghostty-web" turned out not to be an integration at all: what
survives is two files' worth of ordinary canvas-terminal code, and the
emulator-facing half of each was replaced by upstream calls. It is
probably better read as "borrow ghostty-web's drawing loop and its mouse
state machine, and write the rest", and on that reading the port is a
head start of a few hundred lines and a day, not a dependency. Whether a
product renderer would keep even that is a question for when the fonts
and graphemes work is done, since that is where the port is no further
on than the minimal renderer and where the one defect found lives. That
the seam's `Frame` took a foreign renderer with a shadow-row adapter and
no change of shape is probably the more durable finding.

## M4 verdict

The stop condition has two halves, and neither is hit.

**The browser decodes what the daemon encodes.** Established in the
first half and unchanged by the second: the same `ghostty-vt-small.wasm`
bytes, the daemon's `GHOSTSNP` snapshot, the two-stage `ready()`-then-
history decode, a replica whose text is identical to the daemon's
`screen` on every check, keys and mouse through the WASM encoders, and a
foreign renderer painting the same `Frame`s without the seam changing.

**The renderer route did not cost more than the rest of the PoC
combined.** My estimate: about one working day and 1,850 lines for the
rebase against M0–M4a's 13,000 lines under `src/` and their weeks; about a
week more to reach the whole gap list, against the same. That is an
estimate of effort I did myself, stated as such; the coordinator's own
accounting of the earlier milestones is the other side of the
comparison. What the evidence does say with more confidence: the
proposal's worry — "absorbing a 1,620-line patch against a stale pin" —
did not materialise, because no line of the patch was needed; the cost
that did materialise is the ordinary cost of a terminal renderer's long
tail, which ghostty-web shares rather than removes.
